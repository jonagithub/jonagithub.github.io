# AI Agent Rules & Guidelines

## Git Operations
- DO NOT perform any Git changes (such as `git add`, `git commit`, `git push`, etc.) automatically.
- ALWAYS ask for explicit permission before attempting to run any Git commands or proposing a terminal command to execute them.
